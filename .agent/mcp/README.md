# Agent MCP

Repository-local MCP configuration notes, server references, and setup docs belong here.

Keep shared MCP documentation in this directory so multiple coding agents can use the same source.
